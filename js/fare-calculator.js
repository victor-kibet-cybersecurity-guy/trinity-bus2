document.addEventListener("DOMContentLoaded",()=>{const f=document.getElementById("fareForm");if(!f)return;f.addEventListener("input",()=>{const fare=+f.routeFare.value,adults=+f.adults.value,children=+f.children.value,discount=+f.discount.value;const sub=fare*adults+fare*.65*children,total=Math.max(0,sub-discount);document.getElementById("fareResult").innerHTML=`<p>Adult fare: <strong>${TBE.money(fare*adults)}</strong></p><p>Child fare: <strong>${TBE.money(fare*.65*children)}</strong></p><p>Subtotal: <strong>${TBE.money(sub)}</strong></p><p>Discount: <strong>${TBE.money(discount)}</strong></p><p class="price">Estimated total: ${TBE.money(total)}</p><small>Estimated fare only. Final price is confirmed during booking.</small>`})})